Options -Indexes -ExecCGI

# Папка принимает загружаемые файлы (фото товаров и т.п.) — запрещаем
# исполнение любых PHP-подобных файлов, даже если валидация типа при
# загрузке когда-нибудь будет обойдена.
<FilesMatch "\.ph(p[3457]?|t|tml)$">
    Require all denied
</FilesMatch>
