# «Сдоба» — маркетплейс кондитерских изделий

Интернет-магазин выпечки с мультивендорной моделью: кондитеры-партнёры
размещают товары на общей витрине, покупатель оформляет один заказ на
товары нескольких кондитеров, платформа удерживает комиссию.

Разрабатывается по SDD-методологии: документация первична, код пишется
по ней.

**Стек:** PHP 8.2 + MySQL 8 + Bootstrap 5.3 + jQuery (тема) + свой Vanilla JS
**Деплой:** shared-хостинг, без Node.js и доступа к консоли
**Инструмент разработки:** Claude Code (VS Code) — промпты и правила рассчитаны на него

---

## Быстрый старт

1. Заполни `.env` на основе `.env.example` — доступ к БД, SMTP, ключи ЮKassa
2. `composer install` — подтянет PHPUnit (dev-зависимость, на прод не деплоится)
3. Скопируй `database/install.php` в `public/install-temp.php`, открой в
   браузере, **удали файл после выполнения** — создаст 33 таблицы
4. Открой `.docs/phases/_status.md` — какая фаза в работе
5. Дальше по циклу из `current-pipeline.md`

Документация проекта уже заполнена: `.docs/prd.md` (продукт и
требования), `.docs/database.md` (схема БД), `example-template/README.md`
(карта вёрстки). Промпт `.docs/prompts/project-init.md` нужен только для
старта с нуля — здесь этот шаг пройден.

---

## Структура проекта — что где лежит

```
php-starter-ecommerce/
│
├── CLAUDE.md                    ← Конституция проекта для агента
├── TASK.md                      ← Текущий активный таск
├── TASK-example.md              ← Шаблон для создания тасков
├── current-pipeline.md          ← Схема процесса разработки
├── .env.example                 ← Переменные окружения (образец)
├── .htaccess                    ← Редирект всего в public/
├── composer.json                ← PHPUnit (dev-only), автозагрузка tests/
├── phpunit.xml                  ← Конфиг PHPUnit
│
├── .claude/rules/                ← Правила для Claude Code (подгружаются автоматически)
│   ├── general.md                ← Универсальные правила кода
│   └── php.md                    ← Правила PHP
│
├── .docs/                       ← Вся документация проекта
│   ├── prd.md                   ← Описание продукта (ЗАПОЛНИТЬ)
│   ├── database.md              ← Схема БД (дозаполнить под свой продукт)
│   ├── dod-global.md            ← Глобальный чеклист готовности
│   ├── dev-log.md               ← Журнал сессий
│   ├── phases/
│   │   ├── _status.md           ← Статус всех фаз
│   │   └── phase-example.md     ← Шаблон фазы
│   └── prompts/
│       ├── project-init.md      ← Промпт: ТЗ + скриншоты → prd.md + database.md
│       ├── phase-init.md        ← Промпт для создания фазы
│       └── task-init.md         ← Промпт для создания таска
│
├── example-template/            ← READ-ONLY срез макета Fastkart (37 МБ)
│   ├── README.md                ← Карта: файл макета → маршрут → пункт PRD
│   ├── front-end/               ← 27 страниц витрины
│   ├── back-end/                ← 30 страниц админки + свои ассеты
│   ├── email-templete/          ← 5 шаблонов писем
│   ├── invoice/                 ← Печатная накладная
│   └── assets/                  ← CSS, JS, шрифты, 254 изображения
│                                  Не редактировать. На прод не деплоить
│
├── public/                      ← Document root (сюда смотрит сервер)
│   ├── index.php                ← Front controller — точка входа
│   ├── .htaccess                ← Options -Indexes + фронт-контроллер
│   ├── assets/
│   │   ├── vendor/              ← CSS и JS темы AS IS — не редактируем
│   │   ├── css/                 ← Свой CSS поверх темы, строго BEM
│   │   ├── js/                  ← Свой JS, ES-модули, без jQuery
│   │   └── img/
│   └── uploads/                 ← Загружаемый контент (фото товаров и т.п.)
│       ├── .htaccess            ← Запрет выполнения PHP в этой папке
│       └── products/
│
├── src/                         ← Весь PHP код
│   ├── Controllers/             ← Логика обработки запросов
│   ├── Models/                  ← Работа с БД
│   ├── Views/                   ← HTML шаблоны
│   │   ├── shop/                ← Витрина: свой layout и компоненты
│   │   └── admin/               ← Админка: свой layout и свои ассеты
│   ├── Services/                ← Сервисы (email, файлы и т.д.)
│   └── Core/                   ← Ядро приложения
│       ├── Router.php
│       ├── Database.php
│       ├── Request.php
│       ├── functions.php
│       ├── Logger.php
│       └── ErrorHandlers.php
│
├── config/
│   ├── config.php               ← Константы и настройки
│   └── routes.php               ← Все маршруты
│
├── database/
│   ├── install.php              ← Создание 33 таблиц + системные справочники
│   └── seed.php                 ← Демо-контент (появится в Фазе 1)
│
├── tests/
│   ├── bootstrap.php            ← Подключает Core-файлы для PHPUnit
│   └── Unit/                    ← Тесты чистой логики (Core-хелперы, Router)
│
└── storage/
    ├── logs/                    ← Логи приложения
    ├── cache/
    └── temp/
```

---

## Состояние документации

### Заполнено под проект:

| Файл | Что внутри |
|------|-----------|
| `.docs/prd.md` | Продукт, роли, 63 страницы с привязкой к макету, 79 требований, роадмап на 10 фаз |
| `.docs/database.md` | 33 таблицы: 8 базовых расширены, 25 новых |
| `database/install.php` | Схема зеркалирована, 46 внешних ключей |
| `example-template/` | Срез макета + карта соответствий страниц |
| `CLAUDE.md` | Стек, архитектура, правила слоёв под этот проект |
| `.claude/rules/general.md` | Исключения под тему: jQuery в вендорном слое, BEM для своего CSS |
| `.docs/phases/_status.md` | 10 фаз |

### Осталось заполнить:

| Файл | Что сделать |
|------|------------|
| `.env` | Доступ к БД, SMTP, ключи тестового магазина ЮKassa |
| `.docs/phases/phase-0.md` | Создать промптом из `.docs/prompts/phase-init.md` |

### Универсальные файлы — не трогать:

| Файл | Почему не трогать |
|------|------------------|
| `src/Core/*.php` | Работает для любого проекта |
| `.htaccess`, `public/.htaccess` | Редирект + `Options -Indexes` (запрет листинга директорий) |
| `public/uploads/.htaccess` | Запрещает выполнение PHP в папке загрузок — защита от RCE через подделанный тип файла. Не удалять и не ослаблять |
| `public/index.php` | Универсальный front controller |
| `.claude/rules/php.md` | Правила PHP универсальны |
| `.docs/dod-global.md` | Подходит для любого PHP проекта |
| `.docs/prompts/*.md` | Промпты универсальны |
| `phpunit.xml`, `tests/bootstrap.php` | Конфиг тестов универсален |

**Про `storage/` vs `public/uploads/`:** `storage/` лежит вне document root
и по HTTP недоступна в принципе — туда идут логи, кэш, временные файлы.
`public/uploads/` — единственное место, куда можно класть контент, который
должен открываться прямой ссылкой (фото товаров), и единственное место, где
пользовательская загрузка в принципе возможна. Именно поэтому там отдельный
`.htaccess`, запрещающий исполнение PHP.

### Очистить под свой проект:

| Файл | Что очистить |
|------|-------------|
| `config/routes.php` | Убрать все маршруты кроме / и /login |
| `database/install.php` | Убрать все таблицы, оставить только структуру подключения |
| `TASK.md` | Очистить полностью |
| `.docs/dev-log.md` | Очистить записи |
| `.docs/phases/_status.md` | Очистить фазы |

---

## Как работать с системой — шпаргалка

### Инициализация нового проекта (один раз)
0. Есть ТЗ и/или скриншоты интерфейса → скопировать промпт из
   `.docs/prompts/project-init.md`, отправить в чат Claude Code вместе
   с ТЗ/скриншотами → агент заполняет `prd.md` и дополняет
   `database.md` → подтвердить

### Начало сессии
1. Открыть `current-pipeline.md` → найти ссылку на `_status.md`
2. Открыть `_status.md` → понять какая фаза в работе
3. Открыть `TASK.md` → прочитать что делать сегодня

### Планирование новой фазы
4. Скопировать промпт из `.docs/prompts/phase-init.md`
5. Отправить в чат Claude Code → агент предлагает план → подтвердить

### Создание таска
6. Скопировать промпт из `.docs/prompts/task-init.md`
7. Отправить в чат Claude Code → агент создаёт TASK.md → подтвердить

### Реализация
8. Сказать агенту: `Реализуй задачу из TASK.md. Следуй CLAUDE.md`
9. Агент пишет код

### Проверка
10. Проверить DoD вручную в браузере — каждый пункт из TASK.md
11. Если что-то не работает → сказать агенту конкретно что не так

### Закрытие таска
12. Поставить `[x]` в DoD в `TASK.md`
13. Обновить статус таска в `phase-N.md`
14. Если есть ещё таски → вернуться к шагу 6

### Закрытие фазы
15. Попросить агента создать отчёт: `Прочитай phase-N.md и создай phase-N-report.md`
16. Обновить `_status.md` → фаза ✅ Завершена, следующая 🔄 В работе

### Конец сессии
17. Написать в `.docs/dev-log.md`: дата, что сделал, что следующее
